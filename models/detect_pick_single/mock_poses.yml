# Poses the mock locate server answers with, keyed by the scene IRI the goal asks for.
# The cube's observed rest pose in the arm-base frame (base 0.72 m above world): the scenex
# spawns it at z 0.04 but it settles on the support plane, center at its 0.02 half-size.
frame_id: base_link
objects:
  https://secorolab.github.io/models/scenes/detect-pick-single-mjc/pick_place_graph/cube/cube_origin:
    position: [0.50, 0.0, 0.02]
    orientation_xyzw: [0.0, 0.0, 0.0, 1.0]
  https://secorolab.github.io/models/scenes/detect-pick-single-mjc/pick_place_graph/cube2/cube2_origin:
    position: [0.50, -0.20, 0.02]
    orientation_xyzw: [0.0, 0.0, 0.0, 1.0]
