# Where each agent starts. Keyed by the name the execution context binds.

[dual_agents.arm1]
# Stabilizer: starts tool-down directly over the press point (base1 0.55, -0.05, 0.36),
# so staging needs no large reorientation sweep near the cabinet.
home = [0.09, 0.64, 3.1416, -1.24, 0.0, -1.2616, 0.0]

[dual_agents.arm2]
# Tool arm: drawer_open's home, wrist rolled to the grasp orientation.
home = [0.0, 0.2618, 3.1416, -2.2689, 0.0, 0.9599, 0.0]
