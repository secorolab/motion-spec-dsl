# motion-spec-dsl

`motion-spec-dsl` validates guarded-motion `.robmot` models and generates RDF for the
motion-spec toolchain. See the repository README and
`models/pick_place_single/pick_place_single.robmot` for the maintained syntax and example.
